# MkDocs output
/site/

# Conversion scratch: regenerated by tools/, not worth versioning
/build/

# The raw Wayback captures the site was rebuilt from. Multiple gigabytes of
# unrelated MOTODEV-era pages come with them, so they stay out of the repo;
# see docs/about.md for where they came from and tools/ for how to re-run the
# conversion against them.
/old_website/

__pycache__/
*.pyc
.DS_Store
